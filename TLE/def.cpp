#include <bits/stdc++.h> // Includes most of the necessary DSA headers
#include <cstdio>
#include <cstring>
#include <cmath>
#include <chrono>
#include <complex>
#include <stack>
#include <queue>
#include <deque>
#include <set>
#include <map>
#include <unordered_set>
#include <unordered_map>
#include <list>
#include <algorithm>
#include <numeric>
#include <bitset>
#include <functional> // For hash functions and more
#define endl "\n"
#define ll long long int
#define vi vector<int>
#define vll vector<ll>
#define vvi vector<vi>
#define pii pair<int, int>
#define pll pair<long long, long long>
#define mod 1000000007
#define inf 1000000000000000001
#define all(c) c.begin(), c.end()
#define mp(x, y) make_pair(x, y)
#define mem(a, val) memset(a, val, sizeof(a))
#define eb emplace_back
#define f first
#define s second

// Fast I/O macros
#define fastio() ios::sync_with_stdio(0); cin.tie(0); cout.tie(0)
#define scan(x) scanf("%d", &x)
#define scanll(x) scanf("%lld", &x)
#define print(x) printf("%d\n", x)
#define printll(x) printf("%lld\n", x)

using namespace std;

int main()
{
    fastio(); // Fast input/output enabled

    #ifdef ONLINEJUDGE
        clock_t tStart = clock();  // Start the clock for runtime measurement
        freopen("input.txt", "r", stdin);   // For input redirection from file
        freopen("output.txt", "w", stdout); // For output redirection to file
    #endif





    // Your code logic headers   //PRESS CTRL+B TO RUN THE CODE
        
    int n;
    cin >> n;  // Input an integer
    cout << n; // Output the integer









    #ifdef ONLINEJUDGE
        fprintf(stderr, "\n>> Runtime: %.10fs\n", (double)(clock() - tStart) / CLOCKS_PER_SEC);  // Print runtime
    #endif

    return 0;
}

